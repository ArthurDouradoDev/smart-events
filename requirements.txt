# Aplicação desktop e coleta HTTP
pywebview>=5.3.0
requests>=2.32.3
urllib3>=2.2.2
# O driver e o Chromium usam revisões acopladas. Manter a versão exata evita
# que um ``pip install`` atualize só o pacote Python e invalide o navegador local.
playwright==1.61.0

# Servidor central e importação de planilhas
fastapi>=0.115.0
uvicorn>=0.30.0
python-multipart>=0.0.18
pandas>=2.2.0
openpyxl>=3.1.5
xlrd>=2.0.1

# Desenvolvimento e empacotamento
pytest>=8.3.0
PyInstaller>=6.11.0
